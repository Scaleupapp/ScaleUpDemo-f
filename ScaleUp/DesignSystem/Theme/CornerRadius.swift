import Foundation

enum CornerRadius {
    /// 8pt — Tags, chips
    static let small: CGFloat = 8
    /// 16pt — Cards
    static let medium: CGFloat = 16
    /// 24pt — Modals, sheets
    static let large: CGFloat = 24
    /// 9999pt — Avatars, pills
    static let full: CGFloat = 9999
}
